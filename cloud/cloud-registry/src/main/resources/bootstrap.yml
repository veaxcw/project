spring:
  application:
    name: eureka-server
  cloud:
    config:
      uri: http://localhost:8001
eureka:
  instance:
    hostname: localhost
  client:
    register-with-eureka: false
    fetch-registry: false

  server:
    enable-self-preservation: true
server:
  port: 8001
